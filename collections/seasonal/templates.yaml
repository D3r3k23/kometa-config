templates:
  seasonal:
    default:
      section: '00'
      sort_prefix: '>'
      order: custom
    optional:
    - range
    # schedule: range(<<range>>)
    delete_not_scheduled: true
    sort_title: '!<<section>>-Seasonal-<<sort_prefix>> <<collection_sort>>'
    label: Seasonal
    collection_order: <<order>>
    collection_mode: hide
    visible_library: true
    visible_home: true
    visible_shared: true

  tmdb_holiday:
    plex_all: true
    filters:
      tmdb_keyword: <<keyword>>
