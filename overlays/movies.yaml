external_templates:
  file: config/overlays/templates.yaml

overlays:
  "Movie Backdrop":
    template: {name: backdrop}

  "Favorite Movie":
    template:
    - {name: favorite}
    - {name: label, label: Favorite}

  # "Recently Added":
  #   template:
  #   - {name: recently_added}
  #   - {name: label, label: Recently Added}

  # "Recently Released":
  #   template:
  #   - {name: recently_released}
  #   - {name: label, label: Recently Released}
